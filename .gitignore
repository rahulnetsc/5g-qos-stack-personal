# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtualenvs
.venv/
venv/
env/
ENV/

# Test / type checker caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local outputs (figures in design-docs/figures/ ARE checked in;
# anything else generated at the project root is local).
/*.png
/*.csv
/*.json
/timeseries.png

#claude
.claude

# WP9 sweep outputs: scored CSVs and the gate verdict are committed per
# docs/wp9-plan.md sec7; raw per-cell RunRecord JSONL is not (size), and the
# exact re-run command is recorded in the stage's own commit message.
sweeps/**/records.jsonl
