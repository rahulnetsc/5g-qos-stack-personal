# OAI port map

A spot-checking reference for every behavior ported from the vendored OAI
source (`oai-branches/`) into this simulator so far. One row per mechanism,
ordered **load-bearing first** — WP3's BSR realism is wired into every
scenario's live UL path; WP1's power headroom is dormant (unit-tested only,
not imported by `driver.py` or any scheduler — README §4).

C line ranges are in `oai-branches/two-tier/`, not the external OAI
checkout. Where a mechanism has no OAI counterpart at all (a deliberate
simulator-only addition), the C-source column says so explicitly rather
than citing a nearby-but-unrelated line range.

## WP3 — BSR realism (`sim/bsr.py`, live in every scenario via `sim/driver.py`)

| # | Mechanism | C source | Python | Test(s) | Divergence |
|---|---|---|---|---|---|
| 1 | **Cold-start / re-arm probe — RETIRED by WP4.** Was: when a flow's gated report would be 0 but real data is queued and a BSR is `pending`, report the true backlog directly instead of 0. WP4 replaced this stopgap with the real SR mechanism — see row 9 below (`sim/ul_access.py`). | **None** — no OAI mechanism ported; this row now documents what was removed, not live code. | Was `sim/bsr.py`'s `broadcast()` probe branch (pre-WP4); now `broadcast()` calls `ul_access.sr_report_floor()` instead (`sim/bsr.py:305-341`). | `sim/tests/test_bsr.py::test_broadcast_alone_cannot_report_without_a_grant_or_sr` (confirms the probe's bypass is gone: without SR engaged, `bytes_reported` stays 0, not the true backlog) | **Retired, not simplified away without a replacement** — the deadlock this stood in for is real and is now closed by row 9's real SR mechanism, verified in `sim/tests/test_ul_access.py`. See that row's Divergence cell for what changed in the process (WP4 did not fully restore the probe's own delivery numbers — see README §8). |
| 2 | **`sched_ul_bytes`/`B` collapse-to-crumb gate.** `B = max(0, estimated_ul_buffer − sched_ul_bytes)`; `sched_ul_bytes += tb_size` on every grant, reset to 0 only when a BSR is actually assembled. | `gNB_scheduler_ulsch.c:2151`, `:2460` (`B` computed in `pf_ul`), `:2730` (`sched_ul_bytes += tb_size` in `post_process_ulsch`), `:630`, `:651` (reset to 0, short/long BSR cases) | `sim/bsr.py:236-238` (credit, in `on_ul_grant`), `:268` (reset), `:295` (`broadcast`'s `B` computation) | `sim/tests/test_bsr.py:188-204` (`test_sched_ul_bytes_resets_to_zero_only_when_pending`), `:283-306` (`test_crumb_collapse_not_defeated_by_the_probe`), `:377-403` (`test_crumb_fraction_emerges_from_fast_grants_slow_bsr`) | Faithful port of the *formula*. Measured crumb *frequency* (0.09% of UL grants on `factory_robots_scenario`@1.0×/TwoTier) falls far short of the hardware's ~48-52% — flagged, not chased (README §8, CLAUDE.md known issues). |
| 3 | **Event-triggering: regular / periodic / retx.** A BSR is assembled only when `pending`, set by (a) an arrival on a previously-empty LCG or of higher priority than every other buffered LCG, (b) the periodic (5 ms) timer, or (c) the retx (80 ms) timer. The retx timer restarts on *every* grant, not just BSR-carrying ones. | **Trigger logic and timer values are not in the vendored gNB C at all** — periodicBSR/retxBSR are UE-side MAC timers (`nr_ue_scheduler.c`/UE MAC, not vendored here); the gNB C only ever *receives* whatever BSR arrives. The 5 ms / 80 ms values and the "retx restarts on every grant, so a crumb trickle suppresses recovery" behavior are stated as hardware-measured facts in the charter (README §4 WP3, `p5g-sim-plan.md` §9), not read off a C line. | `sim/bsr.py:172-204` (`on_arrivals`), `:206-212` (`tick_timers`), `:238` (retx restart, inside `on_ul_grant`) | `sim/tests/test_bsr.py:312-324` (`test_regular_trigger_fires_on_previously_empty_lcg`), `:327-334` (`test_no_regular_trigger_without_an_arrival`), `:337-349` (`test_periodic_timer_fires_pending_after_deadline`), `:352-374` (`test_retx_timer_restarts_on_every_grant_suppressing_recovery`) | **No C source to check line-for-line against** — this row is a hardware-measurement port, not a code port. See judgment-call list below for a sub-nuance in the "higher priority" condition that isn't covered by any test. |
| 4 | **Short vs. Long BSR format selection + short-BSR aliasing.** Exactly one active LCG → Short BSR (that LCG's estimate set, every other LCG's slot zeroed). ≥2 active LCGs → Long BSR (all active LCGs' slots set from the same reset-then-repopulate memset). | `gNB_scheduler_ulsch.c:626-645` (Short/Short-Truncated BSR case), `:647-679` (Long/Long-Truncated BSR case) — both `memset` the per-LCG array to 0 before repopulating | `sim/bsr.py:244-266` (inside `on_ul_grant`, the `active_lcgs`/format-selection branch) | `sim/tests/test_bsr.py:126-144` (`test_short_bsr_used_and_other_lcgs_zeroed_when_one_lcg_active`), `:147-162` (`test_long_bsr_used_when_multiple_lcgs_active`), `:165-185` (`test_per_lcg_estimate_frozen_between_grants_not_drained`) | Format selection here is always computed from **true current** per-LCG state, never from a truncation-forced choice (grant too small for a Long BSR CE) — that hazard isn't modeled. See judgment-call list. |
| 5 | **Quantisation: UE-side ceiling-encode + gNB-side +1 overestimation bump.** `quantise_short`/`quantise_long` = encode (smallest table index ≥ true bytes) then decode (+1 index, "to account for headers", index 0 exempt). | Tables: `nr_mac_common.c:43-48` (short, 32 entries), `:57-74` (long, 256 entries). Encoder: `nr_ue_scheduler.c:1506-1542` (`nr_locate_BsrIndexByBufferSize`). Decoder bump: `gNB_scheduler_ulsch.c:239-245` (`overestim_bsr_index`); wrapper functions `:247-258` / `:260-291` (`estimate_ul_buffer_{short,long}_bsr`) | Tables: `sim/bsr.py:53-58`, `:62-79`. Encoder: `:82-90` (`_locate_bsr_index`). Decoder bump: `:93-101` (`_overestim_index`). Composition: `:104-122` (`quantise_short`/`quantise_long`) | `sim/tests/test_bsr.py:37-44`, `:47-51` (table byte-for-byte vs. vendored source, re-checked every test run), `:54-59`, `:62-69`, `:72-74`, `:77-82`, `:85-89`, `:92-99` | None known — tables verified byte-for-byte against the vendored C by an automated test, not just at authoring time. |
| 6 | **Scalar `estimated_ul_buffer` SDU-receipt decrement (finding b).** The scalar decrements by `delivered_bytes` on *every* grant, independent of whether that grant also carries a BSR; the per-LCG array is untouched by this, so the two legitimately desync between BSRs. | `gNB_scheduler_ulsch.c:544-547` (DTCH/DCCH sub-PDU case in `nr_process_mac_pdu`, decrements the scalar only) | `sim/bsr.py:239` (inside `on_ul_grant`, unconditional, before the `pending` check) | `sim/tests/test_bsr.py:207-222` (`test_scalar_decrements_on_every_grant_independent_of_pending`) | Faithful to finding (b) (README §7) with one omission: OAI's `sched_ul_bytes` is *also* decremented at SDU-receipt time (`gNB_scheduler_ulsch.c:1096-1098`), which this port does not mirror — see judgment-call list (the k2/HARQ-pipelining argument for why). |

## WP1 — Tx power headroom (`sim/power.py`, dormant — not imported by `driver.py` or any scheduler)

| # | Mechanism | C source | Python | Test(s) | Divergence |
|---|---|---|---|---|---|
| 7 | **`ph_factor` — 38.213 §7.1.1 PH factor.** `delta_tf` (0 unless `delta_mcs` configured and single-layer) plus `bw_factor` (`10·log10(rb<<mu)`, optional via `include_bw`), rounded half-away-from-zero. | `gNB_scheduler_ulsch.c:208-232` (`compute_ph_factor`). `include_bw` call sites: `:592-599` (true), `:1805-1812`/`:1822-1829`/`:1842-1849` (true, inside `nr_ue_max_mcs_min_rb`), `:2534-2541` (false, PHR telemetry), `:2888-2895` (false, PHR telemetry) | `sim/power.py:22-29` (`_round_half_away_from_zero`), `:32-74` (`ph_factor`) | `sim/tests/test_power.py:18-24`, `:27-34`, `:37-48`, `:51-71`, `:74-85`, `:88-101`, `:104-117` | `roundf()` (half-away-from-zero) explicitly mirrored instead of Python's `round()` (half-to-even) — documented divergence-avoidance, not a divergence. `rb<=0`/`tbs_bits<=0` raise `ValueError` instead of mirroring C's `log10(0.0)`→`-inf`→`int` cast (undefined behavior in C, not deterministic hardware behavior — deliberate raise, already in README §7/CLAUDE.md). |
| 8 | **`shrink_to_power_budget` — two SEQUENTIAL loops, RB first then MCS.** Shrinks RB down to `min_rb` before ever touching MCS; not a joint optimum. | `gNB_scheduler_ulsch.c:1780-1858` (`nr_ue_max_mcs_min_rb`); `AssertFatal`s at `:1789`, `:1790` | `sim/power.py:77-140` | `sim/tests/test_power.py:140-173`, `:176-192`, `:195-207`, `:210-219` | Caller supplies `tbs_bits_fn(rb, mcs)` in place of the C's `nr_compute_tbs()` call (needs a full Qm/code-rate MCS table this sim doesn't have) — every existing test drives it with a synthetic, non-3GPP efficiency table (`_SE_BY_MCS` in `test_power.py`), so only the *loop order* is verified, never a real MCS/TBS number end to end (dormant — no caller exists yet). See judgment-call list. |

## WP4 — Uplink access chain (`sim/ul_access.py`, live in every scenario via `sim/driver.py`)

Ground truth for this WP was **not in `oai-branches/`** when WP4 started —
`gNB_scheduler.c` calls `nr_sr_reporting`/`nr_schedule_pucch` but neither
was defined anywhere in what WP1/WP3 vendored, and the SR-transmission-
attempt counting (`nr_ue_get_SR`) lived in a file (`nr_ue_procedures.c`)
that had never been pulled in. Found and vendored from the live checkout
(`~/projects/Oai_Ran_QoS_Supported_MultiDRB`, `twotier` branch) as part of
this WP; see `oai-branches/README.md` for commit provenance. C line ranges
below are in the newly-vendored `oai-branches/two-tier/gNB_scheduler_uci.c`
and `nr_ue_procedures.c`, plus the already-vendored `nr_ue_scheduler.c`
(WP3) and `gNB_scheduler_ulsch.c` (WP3) where this WP also depends on them.

| # | Mechanism | C source | Python | Test(s) | Divergence |
|---|---|---|---|---|---|
| 9 | **SR trigger.** New data arriving when a UE has no other way to signal it (no standing grant, no SR already pending, not mid-RACH-recovery) arms a pending SR. | `nr_update_sr` (`oai-branches/two-tier/nr_ue_scheduler.c:1207-1277`, already vendored for WP3) | `sim/ul_access.py:118-143` (`UlAccessModel.on_arrivals`) | `sim/tests/test_ul_access.py::test_arrival_on_empty_ue_arms_pending`, `::test_no_trigger_when_buffer_was_already_nonempty` | **Simplified to UE granularity, not per-logical-channel**, and the ground truth's two additional gates (`logicalChannelSR-DelayTimer`, `configuredGrantConfig`+`logicalChannelSR-Mask`) are not ported — no per-LCID SR-DelayTimer or CG-mask config exists in this sim to key them off. Judgment call, recorded below. |
| 10 | **Recurring PUCCH SR occasion + transmission-attempt counter + prohibit-timer gate.** On each `(slot - offset) % period == 0` occasion, if pending and not prohibit-blocked, transmit: increment `counter`, arm the prohibit timer, set the gNB's SR flag. Retransmits (not one-shot) at every subsequent occasion until granted or exhausted. | `trigger_periodic_scheduling_request` (`oai-branches/two-tier/nr_ue_procedures.c:2569-2611`) + `nr_ue_get_SR` (`:2613-2661`, the counter/prohibit-timer logic) | `sim/ul_access.py:145-191` (`UlAccessModel.tick`) | `sim/tests/test_ul_access.py::test_tick_fires_sr_on_the_next_occasion_and_sets_gnb_flag`, `::test_prohibit_timer_suppresses_a_retransmission`, `::test_prohibit_timer_is_a_no_op_at_the_real_deployed_value`, `::test_on_ul_grant_clears_sr_state` | **SR periodicity has no ground truth anywhere** (not the calibration banner, not either OAI repo's config for this deployment) — a first-class swept parameter (`sr_period_slots`), not a silent default (README §8). `sr_ProhibitTimer` at its real deployed value (0 ms, `calibration-logs/twotier_startup_gnb.log`) is a genuine no-op per `nr_timer_start`/`nr_timer_is_active` (OAI `common/utils/nr/nr_common.c:1414-1436` — a plain flag with no minimum-active-duration floor); the suppression test above uses an explicit illustrative 5 ms value instead, since the real value cannot demonstrate the mechanism at all. |
| 11 | **gNB decodes a received SR; clears its flag on any grant.** A positive PUCCH SR sets `sched_ctrl->SR`; the flag is cleared the moment the UE receives any UL grant, regardless of grant size. | Set: `handle_nr_uci_pucch_0_1` (`gNB_scheduler_uci.c:959`, `sched_ctrl->SR \|= true`). Clear: `gNB_scheduler_ulsch.c:2694` (`sched_ctrl->SR = false`, already vendored for WP3). | Set (collapsed with the UE-side transmission, no PHY loss model for PUCCH in this sim): `sim/ul_access.py:187` (inside `tick`). Clear: `sim/ul_access.py:193-202` (`on_ul_grant`) | `sim/tests/test_ul_access.py::test_on_ul_grant_clears_sr_state` | UE transmission and gNB reception are one atomic, lossless step (this sim has no PUCCH PHY model) — real hardware can lose an SR transmission, which is exactly why real UEs retransmit rather than assuming receipt; ported the retransmit *behavior* (row 10) without needing a loss model to motivate it operationally. |
| 12 | **sr-TransMax exhaustion → RACH fallback (timing only).** On exhausting `sr-TransMax` attempts, cancel the pending SR and enter a fixed recovery delay; no preamble-collision modeling (README §6 draws this line). | `nr_ue_get_SR`'s final branch (`nr_ue_procedures.c:2655-2660`, calls `schedule_RA_after_SR_failure`, `nr_ue_scheduler.c:1190-1206`, already vendored) | `sim/ul_access.py:182-190` (exhaustion branch, inside `tick`) | `sim/tests/test_ul_access.py::test_sr_trans_max_exhaustion_falls_back_to_rach` | **The recovery delay's value (t300, 400 ms) is a judgment call**, not a literal port — real RACH's own timing (preamble→RAR→Msg3→contention resolution) is out of scope entirely (README §6); t300 (the RRC-setup response timer, already cited in README §6/§7 from the same calibration banner) was chosen as the nearest real, cited timer of the right order of magnitude ("the mechanism behind multi-hundred-millisecond blackouts", `p5g-sim-plan.md` §9), not because t300 itself governs SR-exhaustion recovery in the spec. |
| 13 | **SR-triggered grant's report floor — a small, honest report, not a lie.** Once served (gNB flag set), `broadcast()` reports a small fixed constant instead of WP3's probe's bypass to the true backlog. | **None** — no OAI counterpart for the exact byte value; grounded instead in this branch's own established "crumb" definition (README/CLAUDE.md known issues: grants ≤150 bytes, ~72-107 byte hardware range) rather than a literal `min_rb`/PRB-capacity C computation this sim doesn't model at the byte level. | `sim/ul_access.py::DEFAULT_SR_REPORT_FLOOR_BYTES = 150`, `sim/ul_access.py:204-213` (`sr_report_floor`) | `sim/tests/test_ul_access.py::test_cold_start_rearms_after_a_flow_drains_and_refills`, `::test_sr_retires_the_cold_start_deadlock_without_it`, `::test_sr_preserves_delivery_on_the_branch_s_main_scenario` | **Tuned once, empirically, from 1 byte to 150 during this WP** — recorded here because it's exactly the kind of number a future session could "simplify" back down without realizing why. At 1 byte, every scheduler's own `tbs_bytes = min(ue_backlog, ...)` sizing cap wasted almost an entire grant's real PRB capacity delivering nothing (`sensor_dense_scenario`+PF mean delivery 50.6%); at 150 bytes (matching the branch's own crumb ceiling) most small messages complete in the triggering grant itself (mean delivery 82.1%). Still short of WP3 probe's 99.1% — see row 1's Divergence cell and README §8. |

---

## Worked numeric trace: BSR path, one UE, two LCGs, 4000 bytes

Every number below is the actual output of `sim.bsr.BsrModel`/`sim.buffer.BufferModel`
run against this scenario (not hand-derived and then transcribed) — check the
table lookups against `oai-branches/two-tier/nr_mac_common.c:57-74`
(`NR_LONG_BSR_TABLE`) by hand; everything downstream of the first BSR follows
mechanically from the formulas in rows 1-6 above.

**Setup.** UE 1, two UL flows, `slot_duration_s = 0.0005` (μ=1 → periodicBSR
= 10 slots, retxBSR = 160 slots):

| Flow | qfi | LCG | priority_level | true backlog at t=0 |
|---|---|---|---|---|
| A | 2 | 0 | 40 (higher priority) | 2500 bytes |
| B | 9 | 1 | 90 (lower priority) | 1500 bytes |

Total: **4000 bytes across 2 LCGs.**

### Slot 0 — arrival, regular trigger, first grant

Both LCGs go from empty to non-empty in the same slot → `on_arrivals` fires
the regular trigger (condition (ii)) for **both**: `pending = True`.

A grant of `tb_size = 1000` arrives. The UE's own LCP (not modeled by
`BsrModel` — TS 38.321 §5.4.3.1 is UE-side) splits it 700/300 by priority,
illustratively:

- Flow A: `2500 − 700 = 1800` bytes remaining
- Flow B: `1500 − 300 = 1200` bytes remaining

`on_ul_grant(tb_size=1000, delivered_bytes=1000)` runs. Two LCGs are active
→ **Long BSR**. Per-LCG encode/decode (`oai-branches/two-tier/
nr_mac_common.c:57-74`, `NR_LONG_BSR_TABLE`):

| LCG | true bytes | encode: smallest idx with `table[idx] ≥ true` | encode value | decode: `+1` idx (headroom bump) | decode value = `quantise_long()` |
|---|---|---|---|---|---|
| 0 (A) | 1800 | idx 83 | `table[83] = 1850` | idx 84 | `table[84] = 1970` |
| 1 (B) | 1200 | idx 77 | `table[77] = 1269` | idx 78 | `table[78] = 1351` |

Resulting state:

```
estimated_ul_buffer_per_lcg = [1970, 1351, 0, 0, 0, 0, 0, 0]
estimated_ul_buffer (scalar) = 1970 + 1351 = 3321
sched_ul_bytes = 0            (reset -- a BSR was just assembled)
pending = False
periodic_deadline_slot = 10   (0 + 10)
```

`broadcast()`: `B = max(0, 3321 − 0) = 3321`.

| Flow | `estimated_ul_buffer_per_lcg` | `gated = min(per_lcg, B)` | `bytes_reported` |
|---|---|---|---|
| A | 1970 | min(1970, 3321) = 1970 | **1970** |
| B | 1351 | min(1351, 3321) = 1351 | **1351** |

### Slot 5 — second grant, no new BSR, the crumb-collapse gate fires

No new arrival, no timer due yet → `pending` is still `False`. A grant of
`tb_size = 2000` arrives and fully drains flow A's remaining true backlog
(1800 of the 2000 granted bytes; 200 bytes go unused — flow B is untouched,
still sitting at 1200 true bytes).

`on_ul_grant(tb_size=2000, delivered_bytes=1800)` runs. **Not pending**, so
only the always-on effects apply:

```
sched_ul_bytes += 2000        -> sched_ul_bytes = 2000
estimated_ul_buffer -= 1800   -> estimated_ul_buffer = 3321 - 1800 = 1521   (finding b)
estimated_ul_buffer_per_lcg UNCHANGED: [1970, 1351, 0, 0, 0, 0, 0, 0]        (finding a, frozen)
```

`broadcast()`: `B = max(0, 1521 − 2000) = 0` — **collapsed**.

| Flow | `estimated_ul_buffer_per_lcg` (frozen) | `gated = min(per_lcg, B)` | `bytes_reported` |
|---|---|---|---|
| A | 1970 | min(1970, 0) = 0 | **0** |
| B | 1351 | min(1351, 0) = 0 | **0** |

Note what just happened: flow B's `bytes_reported` collapsed to 0 even
though **nothing has ever touched flow B's true 1200-byte backlog** — it's
purely a side effect of flow A's grant pushing the UE-wide `sched_ul_bytes`
past the UE-wide `estimated_ul_buffer`. This is the mechanism WP3 exists to
demonstrate (README §4 WP3, `p5g-sim-plan.md` §9): a UE-wide gate, not a
per-flow one, gated by a stale aggregate estimate.

### Slots 6-9 — still collapsed, no recovery path fires

No grant, no arrival, neither timer due (`periodic_deadline_slot=10`,
`retx_deadline_slot=160`). `pending` stays `False`; `bytes_reported` stays
at `0` for both flows every slot.

### Slot 10 — periodic timer fires; before WP4, flow B recovered here

`tick_timers(10)`: `10 >= periodic_deadline_slot (10)` → `pending = True`
(BSR's own pending, independent of SR — see the WP4 trace below for SR's
own state machine).

Before WP4, `broadcast()`'s cold-start/re-arm probe fired here (`gated ≤ 0
and bytes_queued > 0 and pending`), reporting flow B's raw true backlog
(1200) directly. That probe is retired (row 1, above). Now, `broadcast()`
asks `ul_access.sr_report_floor(1)` instead: if this UE's own SR state
machine has already fired and is awaiting a grant, it reports the fixed
150-byte floor; otherwise 0. Which one actually happens depends on when
flow B's SR was triggered relative to this slot — worked in full in the
WP4 trace below, which continues from a fresh cold start (flow B's true
backlog going from empty to 1200 bytes) rather than this exact slot-10
moment, since SR's own timing (occasion period, prohibit timer) needs its
own worked example independent of BSR's periodic-timer coincidence here.

---

## Phase 2 — reservation (`scheduler/reservation.py`, new)

Unlike every WP above, no prior Python existed for this scheduler at all
before Phase 2 (`docs/phase2-plan.md`). Rows are added commit by commit as
each mechanism lands, continuing the running row numbering from WP4's
table above. C line ranges are in `oai-branches/reservation/`, not
`two-tier/` — the two branches' `gNB_scheduler_{dlsch,ulsch}.c` have
different contents (`oai-branches/README.md`).

| # | Mechanism | C source | Python | Test(s) | Divergence |
|---|---|---|---|---|---|
| 14 | Per-UE throughput EWMA (`ul_thr_ue`/`dl_thr_ue`), α=0.01, units bytes, updated only for UEs eligible (backlogged) this slot — not a blanket every-UE-every-slot pass. | `gNB_scheduler_ulsch.c:2083-2087`, `gNB_scheduler_dlsch.c:750-752` | `reservation.py::_UeState`, the decay/increment steps inside `_allocate_direction` | `sim/tests/test_reservation.py::test_lower_accumulated_throughput_is_favored_when_prbs_are_scarce`, `::test_pf_coefficient_formula_matches_hand_computation` | `current_bytes`'s accumulation site (grant-time vs. confirmed-delivery-time) isn't visible in the vendored files; ported as `tbs_bytes*(1-bler)` at grant time — the same structural constraint (no post-grant delivery-confirmation callback in `Scheduler`) every existing scheduler resolves the same way. Decay scope (this-slot-eligible UEs only) deliberately does **not** match `sim/baselines/pf.py`'s `_r_avg`, which decays every UE every slot regardless of eligibility (`pf.py:33-35`) — this scheduler's own C ground truth governs here, not the sibling baseline's convention, even though the two look like the same idea. |
| 15 | PF coefficient: hypothetical grant at a **hardcoded** `rbSize=1`, `10`-symbol duration, ÷ thr (floored at 1.0). | `gNB_scheduler_ulsch.c:2205-2213,2301-2302`, `gNB_scheduler_dlsch.c:814-824` (`nr_compute_tbs(Qm, R, 1, 10, 0, 0, 0, layers)`) | `reservation.py::_allocate_direction`'s candidate-building step (`_PF_COEF_HYPOTHETICAL_SYMBOLS = 10`) | `sim/tests/test_reservation.py::test_pf_coefficient_formula_matches_hand_computation` | The `10` is a fixed literal, confirmed to mean 10 *symbols* (matching `nr_compute_tbs`'s 4th parameter at every other call site in this codebase, e.g. `sim/power.py`'s `nrOfSymbols`), not 10 slots — the C's own inline comment ("hypothetical number of slots") is itself imprecise, checked directly rather than trusted. Separately: ground truth's `selected_mcs` feeding this TBS is itself `get_mcs_from_bler`'s (OLLA) output, even at this coefficient — this commit substituted an instantaneous static-staircase lookup (`bits_per_prb`) pending OLLA activation (commit 8/9, `docs/phase2-plan.md` D2). Deliberate and temporary, flagged from commit 1 rather than discovered later. **Closed by commit 9** (row 33): the coefficient's hypothetical TBS and the real grant's TBS both now read `bits_per_prb_for_mcs(mcs_index, ...)` off the same persisted, per-candidate MCS index — though since that index's OLLA offset is itself provably 0 (row 33's Divergence cell), the numeric result is unchanged from the "instantaneous static-staircase lookup" this row originally described, not yet the ratcheted value ground truth's own `selected_mcs` would be. |
| 16 | UL grant emission: a single opaque `ue_grant=True` Allocation; the scheduler never computes a per-flow split. | N/A — simulator architecture, not an OAI-specific mechanism | `reservation.py::_emit_grant`'s UL branch | `sim/tests/test_reservation.py::test_ul_emits_a_single_opaque_ue_grant_allocation`, `::test_scheduler_package_never_imports_sim` | None — conforms exactly to the pattern every existing scheduler (PF/RoundRobin/Gradient, and `two_tier.py`'s own fallback path) already uses. `sim/ue_lcp.py` (driver-owned, `sim/driver.py:613`) performs the real per-flow split entirely outside any scheduler's code — confirming `docs/phase2-plan.md`'s D1 needed zero new code, just conformance to this existing convention. |
| 17 | DL LCP fill — **placeholder** (priority ascending, then backlog descending). | — (placeholder; the real structure is `gNB_scheduler_dlsch.c:1394-1463`, a genuine two-pass SRB/DRB loop, ported in commit 6) | `reservation.py::_dl_fill` | `sim/tests/test_reservation.py::test_dl_emits_one_allocation_per_filled_flow_with_real_qfi` | **Superseded by row 31 (commit 6).** Was a deliberate placeholder (priority-sorted, then greedy) — not merely unfinished but implementing a rule the real hardware doesn't have (row 31: DL fill order is never priority-based); kept here per this file's own precedent (row 20) of not deleting a superseded placeholder row, only annotating it. Do not treat this row as portable evidence of fidelity. |
| 18 | UL comparator: 3-of-5 tiers (SRB[no-op] → GBR[coarse] → PDB/coef). Ground truth is genuinely 5 tiers (SRB → liveness → GBR → `sched_inactive`-last → PDB/coef). | `gNB_scheduler_ulsch.c:2010-2039` (comparator), `:2318-2341` (tier population) | `reservation.py::_ul_rank_key` | `sim/tests/test_reservation.py::test_gbr_tier_beats_the_coefficient_tiebreak_ul`, `::test_pdb_beats_the_coefficient_tiebreak_within_the_same_gbr_bucket_ul`, `::test_ul_and_dl_rank_keys_stay_independently_sourced_not_deduped` | **Two tiers are current no-ops, for two different reasons — `README.md` §8's two new `[OPEN: PHASE2]` entries.** `has_srb` (T1): hardcoded `False`, no SRB/RRC-signaling traffic model exists in this simulator at all (permanent limitation, not a future-commit item). `liveness`/`sched_inactive` (T2/T4): need a `do_sched`-equivalent (SR-or-inactivity trigger for a zero-backlog UE) the `Scheduler` protocol doesn't expose; `sim/ul_access.py`'s SR-report-floor is not a usable proxy (fires only when `bytes_queued > 0`, `sim/bsr.py:381-392`). **Hedged, not verified**: exhaustive case analysis of `liveness = sched_inactive && !ul_has_srb` (`:2339`) suggests T4 may never produce a decisive comparator result even in the real C (whenever `sched_inactive=True`, either T1 or T2 already resolves the comparison first) — recorded as my own reading, not instrumented ground truth, and doesn't change what's ported (T4 is implemented exactly as the C runs it regardless). **Correction, commit 3, not a silent fix**: this row's `pdb_ms` field was originally computed from `buffers.hol_delay_s(...)` (head-of-line queueing delay) — wrong. Ground truth's `ul_best_remaining_pdb_ms` (`:2239-2249`, feeding this same comparator field at `:2335`) is **time since the flow's last grant**, a different quantity — a continuously-served flow has small time-since-last-grant regardless of its current HOL age, and vice versa. Commit 3 fixed this by adding real last-grant-slot tracking (`_ue_state[ue_id].ul_lcg_last_grant_slot`); no commit-2 test caught the bug because every commit-2 fixture started from "never granted," where both proxies coincide — the tests were correct tests of the wrong general quantity, only found by re-reading the exact source, not by running anything. See `sim/tests/test_reservation.py::test_ul_pdb_ms_uses_time_since_last_grant_not_hol_delay`. |
| 19 | DL comparator: 3-of-4 tiers (SRB[no-op] → GBR[coarse] → PDB/coef). Ground truth is genuinely 4 tiers (SRB → liveness(TA) → GBR → PDB/coef) — **DL has no `sched_inactive` field or tier at all**, confirmed absent by reading `UEsched_t` directly, not expressed differently. | `gNB_scheduler_dlsch.c:692-715` (comparator), `:830-843` (tier population) | `reservation.py::_dl_rank_key` | `sim/tests/test_reservation.py::test_gbr_tier_beats_the_coefficient_tiebreak_dl`, `::test_pdb_beats_the_coefficient_tiebreak_within_the_same_gbr_bucket_dl`, `::test_coefficient_remains_the_final_tiebreak_when_gbr_and_pdb_are_equal_dl`, `::test_ul_and_dl_rank_keys_stay_independently_sourced_not_deduped` | Same two no-op categories as UL's row above (`has_srb`: no traffic model; `liveness`: here TA-pending, not SR/inactivity — `gNB_scheduler_dlsch.c:840`, and this simulator has no TA modeling at all). **`_dl_rank_key` is a separately-written, separately-cited method from `_ul_rank_key`, even though their output tuples currently coincide in shape** — a data-availability coincidence (both directions happen to have exactly one real, implementable tier today), not a decision to share a comparator; guarded by its own anti-dedup test. **Same `pdb_ms` correction as row 18** applies here (`dl_best_remaining_pdb_ms`, `gNB_scheduler_dlsch.c:358-367`, also fixed in commit 3) — see `sim/tests/test_reservation.py::test_dl_pdb_ms_uses_time_since_last_grant_not_hol_delay`. |
| 20 | `has_gbr` — coarse placeholder ("any GBR-class flow has reported backlog"). | `gNB_scheduler_ulsch.c:2336` (`ul_has_unfulfilled_gbr`, set inside the per-LCG deficit loop), `gNB_scheduler_dlsch.c:838` (`dl_has_unfulfilled_gbr`, set inside the per-LC deficit loop) | `reservation.py::_allocate_direction`'s `has_gbr` computation (commit 2) | `sim/tests/test_reservation.py::test_gbr_tier_beats_the_coefficient_tiebreak_ul`, `::test_gbr_tier_beats_the_coefficient_tiebreak_dl` | **Superseded by rows 21-22 (commit 3).** Was a deliberate placeholder ("has a GBR flow with any backlog," not real unfulfilled-*deficit* tracking); kept here per this file's own precedent (row 17) of not deleting a placeholder row once superseded, only annotating it. |
| 21 | UL GBR/BE byte split: obligation floored at 1, deficit accumulate/cap at one PDB-window, target spread capped at 2×max-burst, overflow beyond target to BE. Gated per-LCG on `estimated_ul_buffer_per_lcg > 0` for the *whole* block, including accumulation. | `gNB_scheduler_ulsch.c:2251-2278` (formulas), `:2230` (per-LCG gate) | `reservation.py::_ul_gbr_and_pdb` | `sim/tests/test_reservation.py::test_ul_obligation_floors_at_one_byte`, `::test_ul_deficit_caps_at_one_pdb_window`, `::test_ul_target_capped_at_2x_obligation_floor_without_mfbr`, `::test_ul_target_can_exceed_the_floor_when_mfbr_is_configured`, `::test_ul_overflow_beyond_target_credited_to_be`, `::test_ul_has_gbr_requires_a_real_gfbr_not_just_the_flow_class_label`, `::test_ul_deficit_freezes_when_the_per_lcg_estimate_is_zero` | Per-LCG, first-flow-found wins a shared LCG (matching the C's own `lc_config` linear-scan-then-`break`, `:2232-2234,2282`) — dormant, unexercised by any current scenario, the same H5 gap `README.md` §8 already names for BSR aliasing, now confirmed to affect deficit tracking too, not only short-BSR aliasing. `slots_per_sec` is derived from `self.slot_duration_s` rather than reading a hardcoded `spf` — see row 22's DL contrast. `guaranteed_bytes`/`be_bytes` are now consumed by grant sizing (row 25, commit 4a) — this row's divergence note previously said "not yet consumed"; that was accurate through commit 3 only. |
| 22 | DL GBR/BE byte split: same four formulas, but deficit **accumulates unconditionally** for every GBR-configured LCID — a real asymmetry from UL, not shared code with an incidentally-matching arithmetic. | `gNB_scheduler_dlsch.c:377-409` (formulas); `:381-388` accumulate/cap/flag unconditional, `:391` gates only target/overflow on `bytes_in_buffer > 0` | `reservation.py::_dl_gbr_and_pdb` | `sim/tests/test_reservation.py::test_dl_deficit_accumulates_through_silence_unlike_ul`, `::test_dl_target_not_computed_while_buffer_is_empty`, `::test_dl_has_gbr_requires_a_real_gfbr_not_just_the_flow_class_label`, `::test_dl_overflow_beyond_target_credited_to_be` | **The DL/UL asymmetry, precisely**: UL's outer per-LCG loop gates the *whole* block (row 21) on `estimated_ul_buffer_per_lcg > 0`, so a UL LCG's deficit freezes at 0 backlog; DL's deficit keeps growing through silence (verified directly by reading both exact ranges, not assumed from the charter's "identical formula" summary — true for the arithmetic, false for when it runs). Separately: DL's C hardcodes `spf=20`/`slot_ms=0.5` with an explicit comment admitting the numerology-1-only assumption (`gNB_scheduler_dlsch.c:355-356`, `/* matches your rancfg */`), unlike UL's C which reads a real `slots_per_frame` variable — the Python port derives `slots_per_sec` from `self.slot_duration_s` for *both* directions (matching UL's own numerology-aware approach), producing the identical number to DL's hardcoded value at this repo's actual default numerology, so this is a robustness fix, not a fidelity deviation — the same category of correction already applied to the Tier-1-period finding earlier in this document. |
| 23 | `FlowConfig.mfbr_bps` — new field, MFBR input to the 2×max-burst cap (rows 21-22). | `gbr_ul_max`/`gbr_dl_max` fields referenced at `gNB_scheduler_ulsch.c:2268`/`_dlsch.c:398` | `scheduler/flow.py::FlowConfig.mfbr_bps` (docs/phase2-plan.md D5) | `sim/tests/test_reservation.py::test_ul_target_can_exceed_the_floor_when_mfbr_is_configured` | `0.0` default means "not configured" — the cap then falls back to its own floor (`2×obligation`), matching the C's own behavior when a QoS profile has no MFBR set. No current scenario sets a nonzero value; the field is additive and every existing `FlowConfig()` call is unaffected. |
| 24 | **Integer arithmetic across the whole deficit block, both directions** — grant age truncated to whole ms before subtraction; `window` truncating the *ratio* (not the product); `rem_slots` truncated then floored at 1; `target` an integer division; deficit stored as int. Plus the `pdb > 0 ? pdb : 300` fallback and the `9999` best-remaining sentinel. | `gNB_scheduler_ulsch.c:2236,2243-2245,2253-2270`, `:2223` (sentinel) / `gNB_scheduler_dlsch.c:353,363-366,379-399`, `:330` (sentinel) | `reservation.py::_ul_gbr_and_pdb`, `::_dl_gbr_and_pdb`, `_PDB_FALLBACK_MS`, `_Candidate.pdb_ms` (now `int`) | `sim/tests/test_reservation.py::test_ul_remaining_pdb_truncates_grant_age_to_whole_milliseconds`, `::test_dl_remaining_pdb_truncates_grant_age_to_whole_milliseconds`, `::test_two_flows_inside_one_millisecond_tie_at_the_pdb_tier`, `::test_rem_slots_truncation_shrinks_the_target`, `::test_window_truncates_the_ratio_not_the_product`, `::test_unconfigured_pdb_falls_back_to_300ms_not_zero`, `::test_no_eligible_flow_reports_the_c_s_own_9999_sentinel` | **Correction to commit 3, not a silent fix — the same treatment rows 18/19 gave commit 2's `pdb_ms` bug.** Commit 3 ported the right *quantity* (time since last grant) in the wrong *type*: float ms where the C is int ms throughout. This is not rounding noise — `pdb_ms` feeds a comparator TIER, so int-ms granularity makes two UEs within the same millisecond TIE there and fall through to the PF coefficient, while a float port separates them at the PDB tier and never reaches the coefficient. Live on the corpus's own numerology (1, 0.5 ms slots) for any odd slot count since a grant. **Why no commit-3 test caught it**: every fixture used ages landing on whole milliseconds, where both ports agree exactly — the identical blind spot that hid the commit-2 bug one commit earlier, and the reason the new fixtures use odd slot counts. It moved one existing expectation (`test_ul_target_can_exceed_the_floor_when_mfbr_is_configured`: 201 → 100 at slot 199), kept in the file as evidence the correction is behavioural rather than cosmetic. **Checked and found symmetric**: all four truncation sites are element-for-element identical between UL and DL — in particular both truncate the window's ratio rather than its product — so this is one shared row, not the fifth UL/DL asymmetry it was scoped as a candidate for. `window`'s truncation is dormant at every real numerology (an integer `pdb_ms` over a 0.5/0.25 ms slot always divides evenly) and is exercised by a synthetic 0.3 ms slot. **Not ported**: the C's SFN-wrap modulo on the slot difference (`(now - last + 1024*spf) % (1024*spf)`) — this simulator's `slot_index` is monotonic and never wraps, so the modulo would be a no-op at best and would silently convert a long idle gap into a short one at worst. |
| 25 | **UL grant-sizing target (commit 4a)**: `ul_target = guaranteed_bytes + be_bytes`, floored at `B` (real backlog), raised to `gbr_bytes_slot` if higher, capped by the `has_srb` control-only cap — replacing backlog-only sizing. `gbr_bytes_slot` itself is a **MAX over LCGs** (not a sum), computed by a loop structurally separate from the deficit loop — no per-LCG dedup, and it lacks the deficit loop's `if (_obl < 1) _obl = 1` floor. That whole loop is additionally gated on `has_pending_gbr`, set by a *third*, separately-deduped scan keyed on `mfbr_bps` (MFBR), not `gfbr_bps`. | `gNB_scheduler_ulsch.c:2492-2512` (`ul_target`/`B_eff`, feeding `nr_ue_max_mcs_min_rb`'s PRB sizing); `:2304-2316` (`gbr_bytes_slot`); `:38-67` (`update_ul_qos_priority`, the `has_pending_gbr` gate) | `reservation.py::_ul_grant_target` (pure function), `::_ul_gbr_bytes_slot`, `::_ul_has_pending_gbr`, `::_ul_lcg0_estimate` | `sim/tests/test_reservation.py::test_ul_target_above_backlog_grants_more_prbs_than_backlog_alone`, `::test_target_below_backlog_leaves_sizing_unchanged`, `::test_ul_gbr_bytes_slot_raises_target_above_guaranteed_plus_be`, `::test_ul_gbr_bytes_slot_is_zero_without_any_mfbr_configured`, `::test_has_srb_cap_is_correct_but_structurally_unreachable`, `::test_sub_one_byte_gbr_floors_to_one_in_deficit_loop_and_zero_in_gbr_bytes_slot` | **D1**: the target sizes PRBs (`prbs_needed = ceil(target*8/bits_per_rb)`), not delivered bytes — `tbs_bytes` stays `min(ue_backlog, capacity)` unchanged. **Three structurally-unreachable sub-mechanisms, each for a different reason, all ported anyway and tested directly through the pure function rather than via a constructed scenario**: (a) `gbr_bytes_slot` is inert on every current scenario for two independent reasons — no scenario configures a nonzero `mfbr_bps` (row 23), and every scenario is single-flow-per-LCG, so the shared-LCG dedup-vs-non-dedup asymmetry that would let it exceed `guaranteed_bytes+be_bytes` never arises (single-LCG: `target >= obligation >= gbr_bytes_slot` always, since `gbr_bytes_slot` is the same per-slot rate as `obligation` minus only the `max(1,...)` floor); (b) the `if (ul_target < B) ul_target = B` floor itself never fires on any single-flow-per-LCG scenario either — `guaranteed_bytes+be_bytes` derives from the *ungated* `estimated_ul_buffer_per_lcg` (so per LCG `guaranteed+be == max(lcg_estimate, target) >= lcg_estimate`) while `B` sums the *gated* `bytes_reported` (`<= estimated_ul_buffer_per_lcg` by construction, WP3), so `guaranteed+be >= B` already holds; reachable only in the shared-LCG case, where `ue_backlog` double-counts a shared LCG's report across flows while the deficit loop counts it once; (c) the `has_srb` cap, same permanent no-op as the sort tier (row 18/19), since `has_srb` is hardcoded `False`. |
| 26 | **DL grant-sizing target (commit 4a)**: `dl_target = guaranteed_bytes + be_bytes + oh`, floored at `num_total_bytes + oh`, capped by the `has_srb` control-only cap (`SRB1+SRB2+oh`). `oh = 3*4 + (ta_apply ? 2 : 0)` is a flat `12` here — no TA model exists in this simulator, so `ta_apply` is always `False`. | `gNB_scheduler_dlsch.c:1003-1019` | `reservation.py::_dl_grant_target` (pure function), `_DL_LCP_FIXED_OVERHEAD_BYTES` | `sim/tests/test_reservation.py::test_dl_grant_target_includes_fixed_overhead_ul_does_not`, `::test_target_below_backlog_leaves_sizing_unchanged`, `::test_has_srb_cap_is_correct_but_structurally_unreachable` | `num_total_bytes` needs no new plumbing — for DL, `bytes_reported == bytes_queued` always (`interfaces.py`), so the existing `ue_backlog` variable already *is* this quantity. The C's own comment is quoted directly for `oh`'s provenance: "Fix me: currently, the RLC does not give us the total number of PDUs awaiting. Therefore, for the time being, we put a fixed overhead of 12 (for 4 PDUs) and optionally + 2 for TA" — a real, cited stopgap in the C itself, not this port inventing one. **UL has no equivalent overhead term at all** (row 25) — a real asymmetry, not an omission. `has_srb`'s cap is the same permanent no-op as row 25's. |
| 27 | **UL follower budget (commit 4)**: caps a UE's grantable PRBs so a saturating BE UE cannot zero a starved UE ranked behind it. `max_rbSize` init `(sched_inactive\|\|has_srb) ? min_rb : bwpSize`; `budget = bwpSize - n_followers_need*min_rb`, floored at `min_rb`, clamped `if max_rbSize>budget: max_rbSize=budget`; post-budget skip if the result is still below `min_rb`. `bwpSize` is a **per-UE STATIC width**, unaffected by same-slot grants to earlier-ranked candidates (occupancy is tracked separately via a `rballoc_mask` bitmap this simulator doesn't have) — ported here as `slot.prb_count`, a slot-wide constant, never a running `prbs_left`. `n_followers_need` comes from `needs_service = (B>0)\|\|has_srb\|\|has_gbr\|\|do_sched`. | `gNB_scheduler_ulsch.c:2414-2437` (budget block), `:2340-2341` (`needs_service`), `:2061` (`min_rb` assignment) | `reservation.py::_ul_follower_budget` (pure function), `::_ul_needs_service` (pure function), the `n_followers_after` suffix count in `_allocate_direction` | `sim/tests/test_reservation.py::test_follower_budget_degenerates_to_unconstrained_at_zero_followers`, `::test_follower_budget_lowers_max_rb_size_when_followers_need_protecting`, `::test_follower_budget_floors_at_min_rb_never_below`, `::test_ul_max_rb_size_init_uses_min_rb_when_has_srb_forced`, `::test_needs_service_non_backlog_terms_are_currently_unreachable`, `::test_follower_budget_visibly_protects_trailing_ues_from_a_saturating_leader` | **`min_rb` (`nrmac->min_grant_prb`) defaults to `5` — a deliberate operator/experimenter choice for the calibration campaign** (set so no UE is starved of a grant and BSRs keep being reported), **not** a coincidence with the config-parser's own `defintval=5` (`MACRLC_nr_paramdef.h:141`, full OAI checkout, not the vendored subset — the four vendored `.c` files have no assignment site at all). Corroborated empirically by 486/486 `NPRB 5` lines in `calibration-logs/twotier_startup_gnb.log`, whose own `CMDLINE` cites the exact deployed conf (`ci-scripts/conf_files/gnb.sa.band78.106prb.rfsim.conf`) with no override present. **Hedged**: that log is a two-tier run; applying the value to reservation is an inference (both branches read the same MACRLC-layer field), not a direct observation — falsified by a reservation-specific config override not present in this checkout. `needs_service` is **structurally always-`True`** in this port today — `_allocate_direction`'s candidate list is pre-filtered to `bytes_reported > 0` before a candidate is ever built, so the backlog term alone decides it every time, making `or has_srb`/`or has_gbr` currently unreachable (same root cause as the `do_sched`/liveness no-op elsewhere in this file — `do_sched` itself is dropped, not passed as an always-False parameter). `sched_inactive` has no stored field anywhere in this module (confirmed absent from `_ul_rank_key`'s own tuple, not merely unused), so the `max_rbSize` init reduces in code to `has_srb ? min_rb : bwp_size`. **Found scoping commit 7**: UL has its own per-beam pre-check at `:2376` (`remainUEs[beam]==0 \|\| n_rb_sched[beam]<min_rb`), structurally identical to DL's `:877` one (row 28) and never previously flagged for UL — **not applicable**, same reason (no beam modeling anywhere in `scheduler/`/`sim/`). **Also found scoping commit 7**: the post-budget skip's OR-condition (`:2437`, `rbStart+min_rb>bwpSize \|\| available_rb<min_rb`) has a **provably redundant** first clause, not merely an unmodeled one — the `available_rb` scan (`:2433-2434`) only increments while `rbStart+available_rb<bwpSize`, so its final value always satisfies `rbStart+available_rb<=bwpSize`; therefore `available_rb>=min_rb` already implies `rbStart+min_rb<=bwpSize`, i.e. the first clause can never independently fire when the second is false. Commit 4's single-clause port (`if max_rb_size < min_rb_here: continue`) is complete, not approximate. `min_rb`'s remaining use sites — `nr_find_nb_rb`'s PRB-search lower bound (`:2518-2534`) and `nr_ue_max_mcs_min_rb`'s power-shrink input (`:2518`) — are independently out of scope: the former is the real TBS-table search this codebase substitutes with `scheduler/link.py`'s staircase everywhere (D2), the latter is WP1's already-dormant `sim/power.py::shrink_to_power_budget`. The keepalive fallback `sched.rbSize = min_rb` (`:2549`) is gated on `sched_inactive\|\|has_srb`, both permanent no-ops already. |
| 28 | **DL follower budget (commit 4)**: same clamp shape as UL, genuinely different base and floor. `max_rbSize` init is a **contiguous-free-RB SCAN from `rbStart`** — unlike UL's static `bwpSize`, this *does* shrink as earlier-ranked candidates consume PRBs in the same slot, so it's ported here as the *current* `prbs_left`, read fresh at each candidate's turn inside the grant loop, never hoisted or captured once for the whole slot. `min_rbSize=5` is a bare C literal (`:850`), a **separate constant** from UL's `min_rb` — not the same kind of quantity even though they coincide numerically. `needs_service = (num_total_bytes>0)\|\|dl_has_srb\|\|ta_apply` has **no `has_gbr` term** — a real asymmetry from UL. A per-beam pre-check (`:877`, `remainUEs`/`n_rb_sched`) is **not applicable** — no beam modeling exists anywhere in `scheduler/`/`sim/` (confirmed by grep, zero hits); a different category from "dormant but ported" — there is no signal here to port at all. | `gNB_scheduler_dlsch.c:909-926` (budget block), `:840-842` (`needs_service`), `:850` (`min_rbSize` literal), `:877` (beam pre-check, not applicable) | `reservation.py::_dl_follower_budget` (pure function), `::_dl_needs_service` (pure function), `_DL_FOLLOWER_MIN_RB_SIZE` | `sim/tests/test_reservation.py::test_follower_budget_degenerates_to_unconstrained_at_zero_followers`, `::test_follower_budget_lowers_max_rb_size_when_followers_need_protecting`, `::test_follower_budget_floors_at_min_rb_never_below`, `::test_dl_post_budget_skip_when_remaining_capacity_below_min_rbsize`, `::test_needs_service_non_backlog_terms_are_currently_unreachable`, `::test_dl_follower_budget_base_reflects_prbs_already_consumed_this_slot` | `needs_service` is structurally always-`True` here too, same finding as row 27 (DL's own candidate pre-filter is identical). Landing this alongside row 27 required correcting a stale claim in `docs/phase2-plan.md`/`README.md`: `n_followers=0` making "reservation reduce to plain PF" was scoped too broadly — the provable identity is that the follower-budget **clamp** is a no-op at `n_followers_need=0` (`budget=base`, clamp only ever lowers, `base<=base` always), not that all of `Reservation` collapses to `PF` — the sort tiers and 4a's target-based sizing are untouched and remain real differences whenever a GBR deficit is active. Corrected in both documents, not just here. |
| 29 | **UL post-grant deficit drain (commit 5)**, gated `cur_harq->round==0` (initial transmission — a non-issue for this port, see Divergence). Comment at `:2772`, quoted verbatim: *"distribute tb_size drain proportionally across active LCGs."* Code at `:2769-2775` does **not** divide — it subtracts the **full** `tb_size` from every active LCG's deficit independently. **Ported the code, not the comment.** | `gNB_scheduler_ulsch.c:2760-2777` (`post_process_ulsch`) | `reservation.py::_ul_drain_and_stamp` | `sim/tests/test_reservation.py::test_ul_deficit_drains_full_tb_size_per_active_lcg_including_crumb_gated_ones`, `::test_deficit_drain_floors_at_zero_both_directions` | **A found-and-fixed bug in commit 3's own stamping, discovered scoping this commit, in the OPPOSITE direction from the already-known drain bug**: the C's iteration gate is `estimated_ul_buffer_per_lcg[_lcg] > 0` — every active LCG by its true per-LCG BSR estimate — but commit 3's stamp iterated the candidate's `c.flows`, which is filtered to `bytes_reported > 0` (the crumb-gated view). Since `bytes_reported <= estimated_ul_buffer_per_lcg` always (WP3), `c.flows` deduped by LCG is a possibly-**strict subset** of the C's own set: an LCG with a real backlog but a crumb-gated zero report was silently skipped — **under-stamped** (a stale `last_grant_slot` inflates that LCG's age, shrinking `remaining_pdb` and raising its urgency at the PDB comparator tier once commit 10 wires this in — live ordering behaviour, not just bookkeeping) and **under-drained**. Fixed by iterating `self._flows` directly, deduped by `lcg`, gated on `estimated_ul_buffer_per_lcg > 0` — the same pattern `_ul_gbr_bytes_slot` already uses for the identical reason (commit 4a). Dormant on every current scenario (needs the same shared-LCG construction as the H5 follow-up), tested directly with a fixture built specifically on the divergent path (one normally-reported LCG, one crumb-gated-to-zero LCG with a real estimate) rather than one that would pass under the old, buggy iteration too. **Round-gating confirmed non-issue**: every grant `allocate()` emits is, from this port's own perspective, always the `round==0` case — retransmissions are serviced entirely by the driver's HARQ seam before `scheduler.allocate()` is ever called (`docs/phase2-plan.md` sec2.1's existing "confirmed not a porting gap" finding). |
| 30 | **DL post-grant deficit drain (commit 5)**, gated identically to UL (round==0 / "initial transmission" branch — a genuine symmetry, not an asymmetry). "Drain GBR deficit by bytes actually delivered" (`:1451`), and the code does exactly that — each LC's deficit drains by the real, per-LC bytes `_dl_fill` actually gave it (`lcid_bytes`), never a shared or aggregate amount. **No comment/code mismatch on DL's side** — confirmed directly, not assumed from the charter; this is the one direction where the existing charter text was already accurate. | `gNB_scheduler_dlsch.c:1451-1460` (per-LC drain, inside the round-gated LCP fill, `:1377` `else` branch) | `reservation.py::_dl_drain_and_stamp`, wired off `_dl_fill`'s own `fills` return value (`_allocate_direction`) | `sim/tests/test_reservation.py::test_dl_deficit_drains_by_the_real_per_flow_delivered_bytes`, `::test_dl_stamp_and_drain_skip_flows_that_got_no_fill_bytes`, `::test_deficit_drain_floors_at_zero_both_directions` | **A found-and-fixed bug in commit 3's own stamping — the mirror image of row 29's UL finding, over-stamping instead of under-stamping.** The C's gate is `lcid_bytes > 0` per LC — only a flow `_dl_fill`'s greedy priority pass actually reached. Commit 3's stamp iterated **all** of `c.flows` unconditionally, a superset whenever the fill doesn't reach every eligible flow: a flow entirely starved of bytes this slot would have been stamped as if freshly served. Fixed by reusing `_dl_fill`'s own `fills` list (already exactly the right per-flow breakdown — a flow with `take==0` is never appended there) for both the stamp and the drain, computed once in the grant loop and passed into `_emit_grant` rather than recomputed. Both this bug and row 29's are folded into commit 5 rather than deferred, since the C updates stamp and drain together off one value (`tb_size` UL, `lcid_bytes` DL) in one conditional block — not two separable changes. **The DRAIN half is dormant on every current scenario** — `dl_flow_deficit_bytes` only ever gets an entry for a GBR-class flow, and no scenario configures a UE with two or more GBR DL flows. **The STAMP half is not purely hypothetical**: `_dl_gbr_and_pdb` folds `remaining_pdb` into `best_remaining_pdb` for *every* DL flow regardless of `flow_class` (gated only on `bytes_queued>0`), reading `dl_flow_last_grant_slot` per flow — `sim/scenarios/scenario_config_6.yml`'s UE 10 already has two DL flows (qfi 9 PF, qfi 82 Delay-class), so this bug would be live there, shifting PDB-tier sort order, the moment commit 10 wires this scheduler in — not a scenario this commit's own tests had to invent from nothing. The guard test (`test_dl_stamp_and_drain_skip_flows_that_got_no_fill_bytes`) was verified to actually fail when reverted to the old blanket-`c.flows` stamp before landing, not just asserted to catch it. |
| 31 | **The real two-pass DL LCP fill (commit 6)**, replacing commit 1's priority-sorted placeholder (row 17). Pass 0 (SRB) is **not applicable** — `FlowConfig` has no SRB representation to gate a filter on at all, a stronger category than "dormant but ported" (same treatment as commit 4's DL beam pre-check). Pass 1 (DRB) fills every flow in **existing declared order** — confirmed directly by reading the loop, not assumed: the only `qsort` in this file is the inter-UE `UE_sched` comparator (`:847`); nothing reorders `lc_config`. `qc->priority` feeds `dl_best_priority` but is never assigned into `UE_sched` (checked every assignment directly, `:834-841`) — log-only (`:828`), zero scheduling effect. | `gNB_scheduler_dlsch.c:1394-1463` (two-pass loop), `:847` (confirms the only `qsort` in the file is elsewhere), `:354`/`:373`/`:828` (`dl_best_priority`'s log-only round-trip) | `reservation.py::_dl_fill` | `sim/tests/test_reservation.py::test_dl_fill_uses_declared_order_not_priority_order`, `::test_dl_fill_excludes_flows_that_get_zero_bytes` | **The placeholder it replaces implemented a rule the real hardware doesn't have** — `priority_level` plays no role in DL fill order (a real asymmetry from UL: `sim/ue_lcp.py` genuinely does sort by `priority_level` for the UE-side uplink fill; `scheduler/flow.py`'s `FIVE_QI_PRIORITY` docstring originally stated its reordering-fragility rationale without this scoping — corrected there directly). The discriminating test (`test_dl_fill_uses_declared_order_not_priority_order`) was built with declaration order and priority order deliberately in tension and verified to actually fail under the old placeholder before landing — every *other* multi-DL-flow fixture in this file happens to have the two orderings agree, which would pass under either rule and prove nothing. **A quantified, directional disagreement with 4a, not just an unmodeled abstraction limit**: the C spends header overhead on both sides of this mechanism — `oh=3*4` at sizing time (`:1003-1019`, ported in 4a as `_DL_LCP_FIXED_OVERHEAD_BYTES`, reserving headroom in the *target* for headers) and `sizeof(NR_MAC_SUBHEADER_LONG)` per RLC chunk at fill time (this commit's range, actually spending that headroom on real per-SDU headers). This port has the first but not the second, so `_dl_fill` treats the entire granted TB as payload, reclaiming 4a's +12-byte header headroom as extra payload instead — this port's fill over-delivers relative to the C by roughly one MAC subheader's worth per SDU (~3 bytes, common single-chunk case), always in the same direction, never less. Not proposing to build the fix (would require changing the payload-only `Allocation.bytes_capacity` abstraction this codebase uses everywhere, not just here) — recorded as a quantified, known divergence. No PBR/token-bucket state involved, confirmed directly (no bucket/PBR reference anywhere in the fill loop; `sim/ue_lcp.py:63`'s own UL-only filter confirms `FlowConfig.pbr_bps`/`bsd_ms` stay UL-only). Commit 5's "one entry per flow that got bytes" stamp/drain contract is preserved unchanged (`if take > 0: fills.append(...)`, re-verified by its own test here, not just inherited). |
| 32 | **A persistent per-UE-per-direction MCS index (commit 8, D2(a))** — link adaptation has had no stored home anywhere in this scheduler before this; every prior commit re-derives `bits_per_rb`/`bler` fresh from instantaneous SNR every slot. Computed at candidate-build time, matching the C's own timing (inside the per-UE ranking loop, before the `qsort` — for every candidate, not just the eventual winner). Uses the existing static staircase (`scheduler/link.py`'s new `mcs_index_for_snr`) — not yet consumed by anything; grant sizing still calls `bits_per_prb` directly, unchanged. | `gNB_scheduler_ulsch.c:2192` (`selected_mcs`'s per-candidate timing) | `scheduler/link.py::mcs_index_for_snr` (new, shared — `two_tier.py`'s own future commit needs the identical primitive), `reservation.py::_Candidate`'s candidate-build step, `_UeState.ul_mcs_index`/`dl_mcs_index` | `sim/tests/test_reservation.py::test_mcs_index_for_snr_matches_the_row_walk`, `::test_mcs_index_for_snr_floors_at_zero_below_lowest_threshold`, `::test_ul_and_dl_mcs_index_persisted_at_candidate_build_time`, `::test_mcs_index_recomputed_fresh_every_slot_not_read_then_preserved` | **Doubly inert at landing, for two independent reasons — commit 9 removed only the second, not both (see row 33)**: (1) nothing imports `Reservation` yet (the standing reason every prior commit in this lineage shares, still true after commit 9); (2) the stored value was written every slot but read by nothing in this module (closed by commit 9). Verified directly, not just asserted, at the time: pre-seeding a nonsense index before `allocate()` left the emitted grant byte-for-byte identical (test renamed at commit 9 to `test_mcs_index_recomputed_fresh_every_slot_not_read_then_preserved`, since by commit 9 the field is a real, consumed input and "not yet consumed" is no longer the claim it demonstrates — see row 33 for the guard test that now covers consumption itself). **Checked before writing, per instruction**: `bits_per_prb`/`mcs_threshold_for_snr` both already delegated to one shared `_mcs_row_for_snr` walk, discarding position each time — no existing function returned an index, so `mcs_index_for_snr` is `_mcs_row_for_snr`'s own walk extracted and shared (`_mcs_index_for_snr`, private), not a second independent walk of `_MCS_TABLE`; `_mcs_row_for_snr` now derives from it. Confirmed behavior-preserving: full suite + `--check` re-run clean after the refactor, before any reservation-side change. `sim/olla.py`'s `MCS_INDEX_COUNT=12` already matches `_MCS_TABLE`'s size exactly (WP5 built OLLA against this table from the start) — no table-size reconciliation needed. Floors at index `0` (not `None`) below the lowest SNR threshold, matching `sim/olla.py::init_olla_state`'s own floor-at-`min_mcs` convention — a persisted per-UE field must always be a concrete int, unlike `bits_per_prb`'s "untransmittable" `(0, 1.0)` or `snr_to_prb_floor`'s deliberate raise. |
| 33 | **OLLA's offset from the instantaneous MCS pick (commit 9, D2(b))** — grant sizing (both the PF-coefficient's hypothetical TBS and the real grant's TBS; ground truth's `selected_mcs` feeds both, row 15) now reads `bits_per_prb_for_mcs(mcs_index, symbols)` off the commit-8 persisted index instead of recomputing `bits_per_prb(snr, symbols)` independently — this is what removes commit 8's reason (2). **The ratchet itself (`sim/olla.py::update_mcs_from_bler`) is not called: its offset from `mcs_index_for_snr(snr)` is provably 0 given this scheduler's available inputs, not merely initialized at 0.** Ground truth's `NR_mac_dir_stats_t.rounds[0]`/`[1]` (new-tx/first-retry counts) are incremented at grant-finalization time by the SAME component that issues both new-tx and retry grants (`gNB_scheduler_dlsch.c:1203`/`_ulsch.c:2756`, inside `post_process_dlsch`/the PUSCH PDU build) — a symmetry this simulator's WP5 Decision 4 breaks (retransmission scheduling moved entirely to `sim/driver.py`'s HARQ seam, never reaching `Scheduler.allocate()`; already confirmed directly by this module's own commit-5 finding, row 29's Divergence cell). With `num_dl_sched` permanently 0, the C's own `num_dl_sched <= 3` branch fires every `BLER_UPDATE_FRAME` window and clamps at `min_mcs` from the very first update (`old_mcs == min_mcs` already, `max(old_mcs-1, min_mcs) == min_mcs`) — offset `= mcs - min_mcs == 0`, unconditionally, forever, fully determined without executing the function. | `gNB_scheduler_primitives.c:785-822` (`get_mcs_from_bler`, byte-identical across both branches); `gNB_scheduler_dlsch.c:801-810,1203`, `gNB_scheduler_ulsch.c:2186-2198,2756` (call sites + `rounds[]` increment site) | `scheduler/link.py::bits_per_prb_for_mcs` (new); `reservation.py::_allocate_direction`'s candidate-build step, `_OLLA_OFFSET` constant | `sim/tests/test_reservation.py::test_grant_sizing_now_reads_the_persisted_mcs_index`, `::test_bits_per_prb_for_mcs_matches_the_table_row_directly`, `::test_bits_per_prb_for_mcs_matches_bits_per_prb_via_mcs_index_for_snr`, `::test_bits_per_prb_for_mcs_clamps_out_of_range_index` | **Existing assertions do NOT move** — `_OLLA_OFFSET == 0` makes `bits_per_prb_for_mcs(mcs_index_for_snr(snr), symbols)` numerically identical to the old `bits_per_prb(snr, symbols)` (proven, not just observed, by `test_bits_per_prb_for_mcs_matches_bits_per_prb_via_mcs_index_for_snr`), so commit 8's own MCS-index test and every coefficient/grant-size fixture keep their pre-commit-9 values; confirmed by the full suite + `--check` running clean. Consumption is real despite the unchanged numbers — verified the same way commit 8's own corrupted-index test was, by monkeypatching `bits_per_prb_for_mcs` to a rate `_MCS_TABLE` could never produce and confirming the emitted grant reflects it (`test_grant_sizing_now_reads_the_persisted_mcs_index`), then verified that guard actually fails under a simulated reversion to the old `bits_per_prb(snr, ...)` call before landing, not just asserted. **The below-lowest-MCS-threshold viability gate stays keyed on the raw SNR walk (`bits_per_prb`), not the persisted index** — found scoping this commit: `mcs_index_for_snr` floors at index `0` rather than signaling "no viable MCS" (row 32's own documented convention for a persisted field), so routing the viability gate through it would silently make an arbitrarily-low-SNR UE look transmittable. **The `sim`/`scheduler` boundary question for wiring a live `update_mcs_from_bler` call is deferred, not resolved** — three options (import `sim.olla` directly, duplicate the primitives into `scheduler/link.py`, move `sim/olla.py` into `scheduler/`) were considered and none taken, since no live call site exists yet to justify choosing between them; `README.md` sec8 `[OPEN: WP9]`. **Applies identically to two-tier's own future OLLA commit** — the missing telemetry is a consequence of WP5 Decision 4, not of reservation's scheduling policy, so both arms must land the same offset-pinned-at-zero disposition or a two-tier-vs-reservation comparison would measure "one arm has OLLA, one doesn't." |
| 34 | **Study wiring and baseline capture (commit 10)** — the first port-map row about wiring rather than a single C mechanism, since this is the first commit where anything imports `Reservation`. Added `scheduler.__init__`'s `Reservation` export (matching `TwoTier`'s treatment); a bare `Reservation()` factory in `scripts/scheduler_study.py`/`scripts/regression_corpus.py` (`sim/driver.py:157` calls `scheduler.configure(scenario.flows, grid.slot_duration_s, grid)`, a bare 3-arg call matching the `Scheduler` protocol exactly, so `configure()`'s `min_rb=5` default is never overridden here — already the confirmed deployed value, row 27, not a placeholder); one arm per study (Study 1/2/3), no variant sweep (no analogous config knobs exist yet, unlike `TwoTier`'s 3 Study-1 variants). Study 4 (`study_ul_access_chain`) untouched — its N=2 scenario would reproduce the hardware campaign's own null result for the identical structural reason as `n_followers_need`'s regime boundary, not a new finding; deferred to WP9 (`README.md` sec8 `[OPEN: WP9]`, `f0ae919`). | N/A — a wiring/harness change, not a C mechanism | `scheduler/__init__.py`, `scripts/scheduler_study.py`, `scripts/regression_corpus.py` | `sim/tests/test_smoke.py::test_reservation_smoke_completes` (new — first end-to-end run of this scheduler inside the real driver, real buffers/HARQ/UL-access-chain/`ue_lcp`, replacing every prior commit's synthetic-fixture-only coverage); `regression_corpus.py --check`/`--capture` sequence itself (script-level, no pytest coverage of `scripts/` exists in this repo) | **The follower budget's operating regime was checked empirically, not assumed** — instrumented `Reservation._allocate_direction` and ran it through `sim.driver.run()` on all three scenarios, counting simultaneous per-slot backlogged UEs per direction: factory_robots (Study 1, N=10) 96.7% of UL slots / 12.5% of DL slots have ≥3 simultaneous backlogged UEs; sensor_dense (Study 2, N=30) 70.9% UL / 0.0% DL (no DL traffic at all); latency_bound (Study 3, N=12) 0.0% UL (no UL traffic at all) / 95.2% DL. Neither direction is exercised by all three studies, but the follower budget is well covered in both directions across the set — not a Study-4-style N=2 degenerate case, resolving `f0ae919`'s open question with real counts rather than scenario-config inference. **Falsifiable prediction, scored, not asserted**: `--check` before any code change was clean; after wiring, before `--capture`, `--check` reported exactly 6 mismatches (`study1/overload_mult{1.0,1.5,2.0,3.0}/Reservation`, `study2/pdcch_limited/Reservation`, `study3/latency_bound/Reservation`, each "new case, not in baseline") and zero diffs on any existing record — the gate held, so `--capture` proceeded. Verified at the JSON-value level, not by `git diff` line count (which shows ~7.4k spurious deletions purely from `sort_keys=True` reordering the whole file once new keys interleave alphabetically among existing ones, e.g. "PF" < "Reservation" < "TwoTier" — a text-diff artifact, not a content change): parsed old vs new `records` dicts directly — 6 keys added, 0 removed, 0 of the 22 pre-existing records changed value. Full suite (465 passed, +1 for the new smoke test) and a second post-capture `--check` both clean. **Numbers reported as run, not tuned in response** (`README.md` sec7): Reservation tracks PF closely at every Study 1 point (throughput within ~1-2%, GBR-met count within ±1) despite the sort tiers/target sizing/follower budget all being confirmed live and exercised there; is visibly worse than PF at Study 2 (expected in direction — no SPS/CCE-avoidance mechanism exists here, unlike `TwoTier`); and is the one case with a clear, attributable difference at Study 3 (matches `TwoTier`'s 5-of-8 on-time/99% mean against PF's 88%, via the PDB/GBR sort tiers). Of five live mechanisms, only the sort tiers show a clear effect across these three studies as currently constructed — the follower budget and target-based sizing are confirmed exercised without yet being confirmed differentiating at these operating points, a finding about the port's current shape, not a defect. |
