# The three builds — running document

**Opened 2026-09-08.** Three builds in sequence, then a full re-run of the
guarantee table, then the code audit. Register before each, commit after
each, one fidelity change per commit, **no build netted against another by
argument.** This file is the running record; each build's own registration
lives in its section here and is written BEFORE its diff is looked at.

| # | build | status |
|---|---|---|
| 0a | M-5 LCG renumber (prerequisite of Build 1) | **LANDED** — §1.7; corpus bit-identical, one registered prediction FAILED |
| 0b | SRB capture request to the testbed (prerequisite of Build 1) | **sent** — `docs/requests/srb-capture-request-2026-09-08.md` |
| 1 | RA + SRB | not started |
| 2 | CG prototype (Type 2, UL, divergence arm) | not started; after Build 1's re-run |
| 3 | decide after 1 and 2 | — |
| — | full table re-run, diffed against `docs/GUARANTEE-TABLE-2026-09-07.md` | — |
| — | thorough code audit, scorecard-audit method over the whole codebase | — |

---

## 1. M-5 — LCG = DRB ID: registration

**Registered 2026-09-08, before the diff.**

### 1.1 The deployed rule, from the C

Three facts, each from the full checkout (`Oai_Ran_QoS_Supported_MultiDRB`,
branch `twotier`), none from the vendored subset:

1. **One QoS flow per DRB.** `rrc_gNB_radio_bearers.c:498-505` sets every
   multiplexing limit to 1 — `MAX_QOS_FLOWS_PER_DRB_DC_GBR`, `_GBR`,
   `_NON_GBR` and `_TOTAL` are all `1` — so `nr_rrc_find_suitable_drb_for_qos`
   never reuses a DRB and `nr_rrc_assign_drb_to_qos_flow` creates one per
   flow.
2. **DRB ID = lowest free integer from 1.** `nr_rrc_add_drb`
   (`:248-263`) loops `for (drb_id = 1; ...)` and takes the first unused.
   So DRB IDs are the per-UE **setup ordinal**.
3. **LCG = DRB ID, capped at 7.** `nr_radio_config.c:3781-3785`:
   `long lcg_id = drbId; if (lcg_id > 7) lcg_id = 7;`. The comment
   `// LCG = DRB ID, LCID = DRB ID + 3` appears in **both** branches
   (`gNB_scheduler_ulsch.c:51-52`).

**Consequence: LCG is a per-UE bearer ordinal, not a function of 5QI.**
LCG 0 is never a DRB; it is the SRB group
(`get_SRB_RLC_BearerConfig(..., logicalChannelGroup=0)`). The one line of
calibration-log evidence agrees: `UE 1: created new DRB 1 for QFI 1 (5QI 9)`
→ LCG 1.

### 1.2 What this replaces

`scheduler/flow.py::FIVE_QI_LCG`, an **invented** 5QI→LCG table (its own
comment said so; README §8 carried it as `[OPEN: HARDWARE/DECISION]`). It
put 5QI 1 and 3 on **LCG 0** — the SRB group — which is the M-5 trap: wiring
`has_srb` to the C's own predicate (`per_lcg[0] > 0`) would have read every
telemetry-backlogged UE as having control traffic pending.

### 1.3 Two stated assumptions

- **Setup order = declaration order within the UE.** The C assigns DRB IDs in
  the order QoS flows arrive in the PDU-session setup; the simulator's only
  proxy is the scenario's flow-list order per UE. Every scenario builder
  lists a UE's flows together, so this is well-defined; it is stated because
  nothing in the repo can verify it.
- **A simulator flow is one QoS flow.** Flows here are unidirectional; a
  deployed QoS flow is one DRB carrying both directions. The port shares a
  DRB between a DL and a UL flow with the same `(ue_id, qfi)`; the census
  below finds **zero** such pairs in any built scenario, so the rule has no
  instance today.

### 1.4 Census of every built scenario under the rule (`scratchpad/lcg_census.py`)

19 scenario builds: `scenario(1..6)`, the three parametric mixes plus `bg`
and `shared_lcg`, four fleet compositions at N=8, G12, G11, and the three
G9 builds. Findings:

- **No UE exceeds 7 bearers**, so the cap at 7 never binds and no two data
  flows ever share an LCG under the deployed rule.
- **Zero `(ue, qfi)` pairs in both directions.**
- **Collision structure changes in exactly ONE scenario:**
  `parametric[factory, bg]` — today UE 8's 5QI-9 best-effort flow and the
  5QI-8 aggressor share **LCG 6** (the aggressor set `lcg=6` explicitly,
  which was the 5QI-9 default). Under the deployed rule they are DRB 4 and
  DRB 5, separate LCGs. **This is G6's and G7's cell.**
- `parametric[factory, shared_lcg]` is the H5 divergence axis: it forces
  two UL flows onto one LCG, which the deployed RRC **cannot produce**
  (`MAX_QOS_FLOWS_PER_DRB_TOTAL = 1`). Kept as an explicit override — moved
  from LCG 0 to **LCG 1**, off the SRB group — and labelled a divergence.

### 1.5 The port

- `scheduler/flow.py`: `FIVE_QI_LCG` / `lcg_for_5qi` / `DEFAULT_LCG`
  deleted. `assign_deployed_lcgs(flows)` assigns per-UE DRB ordinals to
  every flow left at `LCG_UNASSIGNED`; `LCG_SRB = 0`, `LCG_MAX_DRB = 7`.
  `FlowConfig.__post_init__` no longer resolves `-1` (it cannot — the
  ordinal depends on the UE's other flows).
- `sim/config.py::ScenarioConfig.__post_init__` is the ONE resolution
  point: it sees the complete flow list. Idempotent, so
  `dataclasses.replace` (G12's `permute_flows`) keeps the resolved LCGs —
  **the permutation control stays a declaration-order control and does not
  silently become an LCG-assignment control.** G11's rotation happens
  before its `ScenarioConfig`, so it resolves first, for the same reason.
- Every consumer that indexes by LCG (`BsrModel`, `Reservation`,
  `TwoTier`, `TrafficModel`) **refuses** an unassigned flow rather than
  indexing `[-1]`, which Python would silently accept as `[7]`.
- Explicit `lcg=6` on the three background aggressors (parametric, G12,
  G9) removed — derived.

### 1.6 Registered predictions

| claim | prediction | can it fail? |
|---|---|---|
| `regression_corpus.py --check` | **bit-identical on all 20 records.** `factory_robots` moves 1/5/6 → 1/2/3 and `sensor_dense` 0 → 1 with no collision change; every LCG-indexed structure is the same data at a different index | **YES** — the corpus stores per-flow UL delivered bytes and system utilisation, and index-sensitive paths exist (Long-BSR ordering, `_lcg_rank`, per-LCG deficit dicts). A diff here is a finding, not a re-baseline |
| every guarantee artefact except G6/G7's | unchanged | yes, same reason |
| **G6 / G7 (bg cell)** | **moves, on UE 8 only**: the aggressor and UE 8's BE flow stop sharing one BSR aggregate and one Reservation deficit counter. Direction not predicted | yes |
| G12's `permute_flows` control | unaffected (LCGs resolve before the shuffle) | yes — a moved G12 order under permutation would show it |
| H5 (`shared_lcg`) cell | identical behaviour at LCG 1 vs LCG 0 (`has_srb` still hardcoded False) | yes |
| `has_srb` | still `False` everywhere — **this commit unlocks nothing on its own**; it removes the trap for Build 1 | — |

**Re-baseline: NONE expected.** If `--check` moves, the diff is read before
anything is captured.

### 1.7 Results — scored against §1.6

| claim | predicted | measured | verdict |
|---|---|---|---|
| `regression_corpus.py --check` | bit-identical, 20 records | **bit-identical** (`OK -- no drift`), run before and after | **HIT**, and it could have failed |
| parametric factory cell (G1/G3/G5/G8/G10's builder) | unchanged | **byte-identical** before/after on 3 arms × seed 1 × 4000 slots, `flows`+`system` hashed, old code in a `git worktree` of HEAD | **HIT** |
| **G6/G7 bg cell — "moves, on UE 8 only"** | moves | **byte-identical**, same probe, all three arms | **MISS** |
| G12 `permute_flows` control | unaffected | mapping identical under three permutation seeds (test) | hit |
| G11 rotation control | unaffected | mapping identical for k ∈ {1,3,5,7} (test) | hit |
| `has_srb` | still False | still False; `_ul_lcg0_estimate` now always 0 (LCG 0 vacant) | — |

**The miss, stated rather than smoothed.** UE 8's 5QI-9 flow and the 5QI-8
aggressor shared LCG 6 and I predicted that separating them would move the
cell. It did not, on any arm. What the probe shows is that the collision was
**inert in every consumer**: both flows are non-GBR with the same PDB, so
Reservation's two array-reading tiers (`has_gbr`, `pdb_ms`) see the same
class either way; per-LCG deficits are GBR-only; and `truncated_bsr="off"`
makes the BSR format depend on the active-LCG count in a way that, with
telemetry/camera/BE also active on UE 8, is Long either way. The prediction
was made from the structure ("they share an aggregate") without asking what
any consumer *does* with the aggregate — the decompose-before-attributing
question, asked of a prediction. **Not investigated further**: a 40-seed
20 000-slot run could still differ at the margin, and G6/G7 are re-run in
full under Build 1 with a before/after column.

**Consequence for the staleness guard.** The seven claims reading
`core_scaled.json` / `g5_consol_scaled.json` were flagged STALE by
`verify_claims` (scoped code changed). Marked `code_state: historical` with
the evidence in the reason — corpus identity plus the builder-cell probe —
rather than re-run; Build 1's full re-run re-stamps them.

### 1.8 The three standing questions (write for the system, not the site)

**1. Who else reads this state?** Every reader of `FlowConfig.lcg`, by grep
over non-test code, and what each needed:

| reader | what it does with the LCG | change |
|---|---|---|
| `sim/bsr.py` (51 sites) | per-LCG arrays, `_lcg_rank`, Long/Short/Truncated format selection | index shift only; **refuses unassigned** |
| `scheduler/reservation.py` (51) | per-LCG deficit / last-grant / `_ul_lcg0_estimate` | index shift; `_ul_lcg0_estimate` now reads a vacant LCG 0; **refuses unassigned** |
| `scheduler/two_tier.py` (86) | per-LCG deficit, `vq_ul`, candidate list | index shift; **refuses unassigned** |
| `sim/traffic.py` → `sim/buffer.py` | `register(..., lcg=)` | **refuses unassigned** |
| `sim/config_loader.py` | YAML default | `LCG_UNASSIGNED`, resolved at `ScenarioConfig` |
| `sim/fleet.py::profile_qos_table` | provenance table | derives via `lcg_for_drb` — the same function, not a restatement |
| `sim/parametric.py` (`shared_lcg`), `sim/scenarios/{g9,g12}.py` (bg `lcg=6`), `g11.py` (rotation) | explicit overrides | overrides removed / moved off LCG 0 / resolved before rotation |
| `scripts/bsr_desync_{probe,study}.py`, `tbs_counterfactual.py`, `g10_*.py`, `analyse_stage3.py`, `wp9_sweep.py` | read `f.lcg` off built scenarios | no literal LCG numbers (grep `lcg == 0`, `per_lcg[0]`: none outside the `has_srb` no-op) — unchanged |

**2. What becomes live that was inert?** Nothing. `has_srb` stays hardcoded
False in both comparators; LCG 0 is now vacant so the predicate *can* be
wired in Build 1 without reading data as control. One thing became
*structurally* different and was measured inert (§1.7's miss).

**3. What does this duplicate?** The rule was about to exist twice —
`assign_deployed_lcgs` and `fleet.py`'s `min(i + 1, 7)` — and now exists
once, `lcg_for_drb`. Resolution exists once (`ScenarioConfig`); the refusal
exists in four consumers but is one function.

**Derive, not list:** the census test ranges over the flow-key sweep's
`_cases()` and inherits its coverage test; it is registered in
`test_enumerator_coverage.ENUMERATORS`. The manipulation check is the guard
demonstrably firing in all four consumers (`test_every_lcg_indexing_consumer_refuses_an_unassigned_flow`).
"Bit-identity when off" has no off switch here; its analogue is the corpus
and the before/after probe above.
